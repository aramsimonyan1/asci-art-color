package main

// drupuje w prawidlowych kolorach w zalenosci czy lettersToColor jest taki sam jak ktorys z wyrazow w textSlice ale grukuje wyrazy w text slice w oddzielnych liniach
/*textSlice := strings.Split(text, " ")
for i, word := range textSlice {
	if word == lettersToColor {
		generate(textSlice[i], lettersToColor, fileLines, colorFlag)
	}
	if word != lettersToColor {
		defaultColorFlag := "white"
		generate(textSlice[i], lettersToColor, fileLines, defaultColorFlag)
	}

}
// tu byl poczatek preLine := []rune(text)
}

func generate(text string, lettersToColor string, fileLines []string, colorFlag string) {
*/
